#pragma once
#include <stdlib.h>

/* ----- DATA TYPES ----- */
typedef unsigned char uint8;
typedef uint8 image[28][28];
typedef float number;

struct Feature;
struct Matrix;
struct Weight;
struct Array;
